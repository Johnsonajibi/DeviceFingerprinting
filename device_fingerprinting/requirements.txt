# Device Fingerprinting Library Requirements

# Post-Quantum Cryptography (Required for PQC features)
pqcdualusb>=0.1.4  # Hybrid post-quantum cryptography library

# Core dependencies
# Note: Basic fingerprinting uses only Python standard library
# PQC features require pqcdualusb

# Development dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.10.0
black>=21.0.0
flake8>=3.8.0
mypy>=0.812

# Documentation dependencies (optional)
sphinx>=3.0.0
sphinx-rtd-theme>=0.5.0

# Build dependencies (optional)
build>=0.7.0
twine>=3.0.0
